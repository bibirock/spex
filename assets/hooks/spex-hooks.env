# spex hook 設定（專案自行維護；安裝時不覆寫既有內容）
#
# 兩支 PostToolUse hook 在代理以 Edit / Write / MultiEdit 異動檔案後，對「剛異動的那一個檔」
# 執行下列指令。留空即停用該支 hook（靜默 exit 0），不會產生噪音。
# 指令以「檔案路徑」為最後一個引數呼叫；可帶旗標，旗標以空白分隔。
# 這裡的值應與 rules/commands.md 記錄的專案指令一致。

# 單檔 lint。有錯誤時以 exit 2 把訊息回報給代理，讓它接著修正。
# 例：SPEX_LINT_COMMAND="npx eslint"
SPEX_LINT_COMMAND=""

# 只對這些副檔名跑 lint（空白分隔的 glob；留空代表不限副檔名）。
# 例：SPEX_LINT_EXTENSIONS="*.ts *.tsx *.js *.jsx *.vue"
SPEX_LINT_EXTENSIONS=""

# 單檔格式化。失敗一律不擋流程。
# 例：SPEX_FORMAT_COMMAND="npx prettier --write"
SPEX_FORMAT_COMMAND=""
