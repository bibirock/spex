{
  "_comment": "由 {{ID}} Sandbox Profile 生成的參考片段（非最終設定檔）。請將下列 hooks 條目手動合併進專案既有的 .claude/settings.json（若尚無 settings.json 則可整份採用，並移除本 _comment 欄位）——render-profile.mjs 不自動深層合併 JSON，以免覆蓋專案既有設定。",
  "_comment_two_hooks": "★ 兩支 PreToolUse hook 並存：本片段的 sandbox-guard.sh 與 spex init 寫入的 spex-stamp-guard.sh 是**不同職責的兩支硬閘**（前者守雙平面邊界，後者守章戳鏈），同一次工具呼叫兩支都會跑，任一 exit 2 即擋。合併規則：(1) 只增不換——絕對不要移除或覆蓋既有的 spex-stamp-guard.sh 條目；(2) 前提是本專案已用 `spex init --mode sandbox` 安裝，該條目命令列尾應為 `--plane sandbox`；若還是 `--plane agent` 或無參數，請重跑 `spex init --mode sandbox`，否則 host 端會拿 session transcript 去驗沙盒內產生的章，必然得到假 FAIL。",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$CLAUDE_PROJECT_DIR/.claude/hooks/sandbox-guard.sh\"",
            "statusMessage": "沙盒平面守門…"
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$CLAUDE_PROJECT_DIR/.claude/hooks/sandbox-guard.sh\"",
            "statusMessage": "沙盒平面守門…"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
{{#IF HAS_POST_EDIT_FORMATTER}}
          {
            "type": "command",
            "command": "bash \"$CLAUDE_PROJECT_DIR/.claude/hooks/format-edited-file.sh\""
          }{{#IF HAS_POST_EDIT_LINTER}},{{/IF}}
{{/IF}}
{{#IF HAS_POST_EDIT_LINTER}}
          {
            "type": "command",
            "command": "bash \"$CLAUDE_PROJECT_DIR/.claude/hooks/lint-edited-file.sh\"",
            "timeout": 60,
            "statusMessage": "Lint 檢查異動檔…"
          }
{{/IF}}
        ]
      }
    ]
  }
}
