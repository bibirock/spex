<!--
由 {{ID}} Sandbox Profile 生成的參考片段（非完整檔案）。請把以下內容合併進專案的
.claude/rules/testing.md 的「E2E / UI 驗證工具」段落——render-profile.mjs 不自動改寫
既有規則檔，以免破壞既有內容。
-->

| 項目                                    | 值                                                                                                 |
| ---------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| **完整 E2E 指令**                        | {{#IF HAS_E2E}}`sandbox/dispatch.sh --e2e`（於沙盒內執行 `{{E2E_COMMAND}}`）{{/IF}} |
| **DB 依賴測試**                          | {{#IF HAS_SIDECAR}}沙盒 sidecar 服務（`sandbox/docker-compose.sandbox.yml`），由 `sandbox/dispatch.sh` 自動啟動{{/IF}} |
| **E2E artifact 目錄（commit 前須清理）** | `coverage/`                                                                                          |

`e2eUnsupportedAlternative`（若此 profile 不支援 --e2e）：{{E2E_UNSUPPORTED_ALTERNATIVE}}
